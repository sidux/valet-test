on:
  pull_request:
    paths:
      - 'file2.yml'
name: Test2
jobs:
  checks:
    name: Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Test
        run: exit 1
